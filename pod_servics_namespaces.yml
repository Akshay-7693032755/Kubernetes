apiversion: v1
kind: pod
metadata: 
  name: acpod
  namespace: ac
  labels:
    app: myapp
spec:
    containers:
        - name: nginx
          image: nginx:lagtest
          ports:
            - containerPort : 80
            protocol : TCP
            name: nginx
---
apiversion: v1
kind: services
metadata: 
    name: b24services
    namespace: ac

spec: 
    type: NodePort
    selector:
      app: myapp
    port:
        - port: 80
          targetport: 80
          protocol: TCP